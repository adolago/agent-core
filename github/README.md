# GitHub Action

This directory contains the GitHub Action implementation for running the CLI in CI.

Documentation is intentionally minimal. See `github/action.yml` and `github/index.ts` for current configuration and defaults.
