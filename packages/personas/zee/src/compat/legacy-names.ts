export const LEGACY_PROJECT_NAME = "zee" as const;

export const LEGACY_MANIFEST_KEY = LEGACY_PROJECT_NAME;

export const LEGACY_PLUGIN_MANIFEST_FILENAME = `${LEGACY_PROJECT_NAME}.plugin.json` as const;
